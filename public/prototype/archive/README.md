# Archived prototype

`app-legacy.html` is the previous static prototype that lived at
`/prototype/app.html`. It is kept here for reference and rollback; the active
Parent Command Center is now served from `../app.html`.
